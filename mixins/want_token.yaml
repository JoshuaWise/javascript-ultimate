%%name%%:
  - match: '{{space}}++'
  - match: '{{line_terminator}}'
    set:
      - match: '{{non_token}}'
      %%~override%%
      %%~overrideNewline%%
      - match: '%%match%%'
        set: %%goto%%
      - match: '{{otherwise}}'
        pop: true
  %%~override%%
  %%~overrideFirstLine%%
  - match: '%%match%%'
    set: %%goto%%
  - match: '%%exitMatch%%'
    pop: true
  - include: otherwise_invalid
