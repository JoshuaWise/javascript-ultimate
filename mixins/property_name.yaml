%%name%%:
  %%~override%%
  - match: '{{ident}}(?={{to_token}}(?:%%~identTail%%))'
    scope: %%identScope%%
    pop: true
  - match: '\['
    set: expression_until_square_bracket
  - include: numeric_literal
  - include: string_literal
